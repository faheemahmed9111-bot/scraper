name: Telegram Bot Poller

on:
  schedule:
    # Runs every 5 minutes to quickly pick up new messages
    - cron: '*/5 * * * *'
  workflow_dispatch: # Allows manual trigger from GitHub Actions tab

jobs:
  poll-telegram:
    name: Poll Telegram Updates
    runs-on: ubuntu-latest
    
    # Prevent concurrent runs to avoid caching issues with the telegram offset
    concurrency:
      group: telegram-poller-group
      cancel-in-progress: false

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install lightweight API dependencies
        run: |
          python -m pip install --upgrade pip
          # We manually list just the lightweight API libraries used by the bot to keep this action incredibly fast
          # and avoid spending 2 minutes installing scrapling and playwright every 5 minutes.
          pip install requests python-dotenv supabase postgrest-py

      - name: Run Telegram Bot
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        run: |
          python telegram_bot.py
